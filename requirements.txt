# ===== 小说爬虫 核心依赖 =====
# 基础爬虫
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
fake-useragent>=1.4.0
urllib3>=2.0.0
certifi>=2023.7.22

# EPUB 导出（抓取完成后可选生成 .epub，依赖 lxml）
ebooklib>=0.19

# 浏览器渲染（tanmixs.com 等强反爬站点必须）
selenium>=4.15.0
playwright>=1.40.0

# 成熟反爬库（多引擎自动降级：curl_cffi TLS 指纹 / cloudscraper JS 质询）
curl_cffi>=0.6.0
cloudscraper>=1.2.0

# 图片 / OCR / 验证码
Pillow>=10.0.0
ddddocr>=1.4.11
opencv-python>=4.8.0
onnxruntime>=1.16.0
numpy>=1.24.0

# ===== GUI 框架 =====
# Flet（基于 Flutter 渲染引擎，支持桌面 EXE 与安卓 APK）
# 注意: GUI 全面使用 0.86 API (page.window / show_dialog / ft.run),
# 0.85 及以下无法运行, 上界防 0.87 破坏性变更
flet>=0.86.0,<0.87
# flet-desktop: flet 的桌面客户端启动器 — flet 只在首次运行 GUI 时运行时
# pip 安装它, 打包机/CI 从未跑过 GUI 就会缺失 → EXE 首启崩溃
# (flet/utils/pip.py 在冻结环境里回退安装时自身 NameError: exit)
flet-desktop>=0.86.0,<0.87

# ===== 远控服务 (源码/远控/, 常驻机 Web 面板; 不打进 EXE) =====
# Python 3.14 兼容性已实测 (fastapi 0.141 / pydantic 2.13)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ===== 打包工具（可选，仅在打 EXE 时使用）=====
# pyinstaller>=6.0.0  # flet pack 内部会用，不手动装也可以
