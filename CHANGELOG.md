# 软件改进记录 (Changelog)

> 本项目每次功能改进均遵循 **「编写/更新测试 → 回归测试 → 打包 EXE」** 发布流程，
> 版本号与 [脚本/build\_exe.py](脚本/build_exe.py) 中的 `PRODUCT_VERSION` 保持一致。
> 按时间倒序排列，最新版本在最上方。

***

## \[2.2.8] - 2026-09-04 (发布)

### 新增

- **速度全自动自适应**（新模块 `源码/速度自适应.py`）：程序依据设备画像
  （CPU 核数 / 可用内存 / CPU 忙碌率 / 单核 sha256 吞吐 / 4 线程扩展比，
  纯标准库，画像缓存 7 天）、任务规模（<30 章压档）与站点约束
  （tanmixs 等反爬敏感站点压档）自动选出最快可用档位并默认以最快速度启动；
  运行中按实时信号动态调节——连续 3 章失败或限频/WAF 事件立即降档，
  冷静期 180s + 连续 30 章成功 + 无风险事件后回升（封顶启动档）；
  并发经动态闸门调节（降档立即收缩、回升立即恢复），章节间隔每章实时生效

- **GUI 移除手动速度选项**：速度完全由程序管理，单任务与批量均默认自适应；
  CLI 保留 `--threads/--delay` 手动直通，交互菜单默认"自动"

### 修复

- 日志 console 镜像与 GUI 任务日志重定向器互递归（1 条日志放大数百次落盘，
  递归护栏断环）
- 站点历史先验对 `delay=None`（自适应默认）抛 TypeError 被吞
- 任务列表标题超 24 字时三元表达式拼反（标题+省略号+全文三段拼接）
- 站点健康度用字符串比较（"100%" < "80%" 导致满分配错误色）
- GUI 状态栏恒假的 `elif failed and not tasks` 死分支
- 批量清单 UTF-8 BOM 导致首行 URL 校验失败（改用 utf-8-sig）
- run_crawl 外层预取 spider 在 full/range 模式从不关闭（Session/浏览器泄漏）
- 请求引擎共享 Session 的 CookieJar 并发竞态（进程级锁串行化引擎路径）
- 并发 worker 获得闸门后复查停止信号（停止后不再发起无谓请求）

### 性能

- 并发抓取线程级 Session 复用：免去每章 TCP/TLS 握手，实测单请求 1682ms →
  824ms（-51%），千章书估算省 ~14 分钟；close() 统一释放
- fake_useragent 进程级单例（多任务不再重复初始化）
- 设备画像基准加锁 + 双重检查（多任务并发首档只跑一份基准），
  缓存路径改用 BASE_DIR（修复 onefile EXE 写入临时目录导致每次重跑）

### 测试

- 回归测试扩至 7 组 37 例全绿（新增：日志递归护栏 / 站点先验 delay=None /
  速度自适应降级回升与并发闸门）；离线解析 29 例全绿；
  322zw.com 真实抓取 6 章集成测试：6/6 成功、质检 100/100、无乱码残留

***

## \[2.1.3] - 2026-09-03 (发布)

### 新增

- **反爬治理**：代理池（多代理按域分配 / 失败降权冷却）、限速自适应（风控事件
  驱动请求间隔 ×因子）、内置 UA 池降级轮换（fake-useragent 失效时按域稳定选取）、
  curl\_cffi TLS 指纹显式化（chrome124）

- **监控闭环**：风控事件打点（反爬/验证码/任务三源 JSONL）、24h 按域聚合告警
  （monitor\_summary）、域级冷却跨 run 自动退避、站点改版漂移检测（EMA 基线 +
  空/短/失败率突变告警）、GUI 告警横幅与站点健康度列

- **调度健壮性**：失败章节外层补试（间隔 3s ×2）+ 并发单章 180s 超时防线

- **新站接入**：`脚本/probe_adapter.py` 探测辅助（URL → 适配草稿）、
  `脚本/replay_consistency.py` 样本回放验收（正文页模式判定一致率 5/5）、
  xingguangks（星光书苑）分卷结构适配、《少年阿宾》124 卷完整抓取验证、
  "找回新域名"类广告通用过滤

### 测试

- 29 例离线回归全绿；8 站实测全过 + xingguangks 完整抓取 124 卷 0 失败

***

## \[2.1.1] - 2026-09-02 (工程维护)

### 修复

- **断点续传丢章漏洞**：章节内容先 `flush` 落盘再保存检查点，消除崩溃时
  "检查点已前进但内容未落盘"导致的续传跳章

- **停止按钮并发假死**：抓取 worker 收到停止信号立即返回 + 取消未开始的
  futures，GUI 停止不再等待全部已提交章节请求

- **资源泄漏**：`NovelSpider.close()` + 上下文管理协议，释放持久化
  Selenium driver 与连接池；多源回退每源抓完即清理

- **TLS 校验开关化**：默认开启证书校验，仅配置 `disable_tls_verify: true` 时关闭

- **码点流解码**：修复压缩字映射丢失、支持 ciyewk 连续码点流

- **重构切块错位**：修复拆分脚本把通用章节提取段误切进 `_resolve_novel_paths`
  导致的运行时 NameError（误入段迁回 `get_chapter_list`）

### 重构

- **巨型方法拆分**：`get_chapter_list` 1,383 行 → 分发器 + 10 站点专属解析器 +
  通用段；`get_chapter_content` 1,346 行 → 模式探测链 + 专用提取方法；
  路径规约抽为模块级纯函数 `_resolve_novel_paths`

### 改进

- **离线回归测试**：24 例基于真实页面快照（qsbs\_bb 解密 / 混淆过滤 /
  正文提取 / 排序 / 清洗 / SSRF 防护），全程不联网

- **代码卫生**：pyflakes 问题 85 → 10（仅剩 PyInstaller 收集导入与
  dataclass 设计的受控保留项）；移除死变量与冗余双栈依赖

***

## \[2.1.0] - 2026-09-02

### 新增功能

- **GUI v3 紧凑信息密度重构**（方案 A）：整体布局由「200px 文字侧边栏 + 4 页签」
  重构为「56px 图标导航栏 + 抓取工作台 + 独立历史页 + 站点管理页 + 日志页」

  - **抓取工作台**：单行紧凑输入条（URL 回车直达开始）+ 全宽任务表格
    （URL / 进度 / 状态 / **引擎 / 反爬 / 耗时 / 质检分** 多列实时指标）+
    行内展开详情（引擎降级链 / 反爬命中 / 质检得分 / 增量跳过 / 错误）+
    可折叠日志条（130px 紧凑态 ⇄ 460px 终端态）+ 右侧上下文抽屉
    （任务详情大进度环 / 抓取结果文件预览）

  - **爬取历史独立页**：5 张统计卡（总请求/新增/更新/未变化/失败）+
    过滤器（站点 / 时间范围 / 结果类型 chips）+ URL 明细表（500 行截断），
    支持「站点汇总」视图与点击站点行按域过滤（对接 `爬取历史.py` / `站点历史.py`）

  - **站点管理页**：站点表格（域名 / 模式 / 健康度·反爬状态 / **启用开关**）

    - 测试连接（后台线程探测，显示状态码/耗时/引擎/反爬识别）+
      底部编辑卡 + 配置导入/导出 JSON

  - **运行时指标中心**：任务表格的引擎/反爬/质检/增量指标由
    `TaskLogRedirector` 从爬虫结构化日志（`[反爬]`/`[质检]`/`[增量]`/`[引擎]` 前缀）
    实时解析回填，爬虫主流程零改动

- **站点探测模块**（[源码/site\_probe.py](源码/site_probe.py)）：requests 探测 →
  反爬检测器识别 → 命中反爬时用请求引擎重试的降级链，输出结构化健康指标

- **运行时站点配置合并**：`站点配置.json` 现在会被爬虫真正加载（按域名 upsert
  覆盖/追加内置 `SITE_PATTERNS`），修复了此前 GUI 配置只写不读的缺口；
  新增 `enabled` 字段支持禁用站点（缺省视为启用，向后兼容）

### 功能优化

- 导航栏 200px → 56px，横向空间全部让渡给任务表格，一屏可见任务数约翻倍

- 状态栏新增任务实时汇总（抓取中/已完成/失败计数 + 状态色圆点）

- 新任务创建后表格立即刷新；任务选中态驱动日志条与抽屉联动

### 问题修复

- 修复 Flet 0.86 兼容问题：`Switch` 不支持 `dense`/`on_click`（改 `on_change`）、
  `SegmentedButton` set 序列化失败（改为互斥切换按钮）、`ft.animation.Animation`
  引用错误

- 修复历史页 `Dropdown` 过滤器在选项变化后选择丢失的问题

### 其他

- 新增模块：`gui_components/icon_rail.py`、`input_bar.py`、`task_table.py`、
  `row_detail.py`、`log_strip.py`、`detail_drawer.py`、
  `gui_components/pages/`（`history_page.py`、`history_data.py`、
  `site_manage_page.py`）、`site_probe.py`

- 新增单测：`脚本/_test_task_metrics.py`（7 项）、`脚本/_test_gui_v3.py`（4 项）
  全部通过；GUI 启动冒烟测试通过

***

## \[2.0.2] - 2026-09-02

### 新增功能

- **爬取历史记录（按 URL 维度）**（[源码/爬取历史.py](源码/爬取历史.py)）

  - 持久化每次请求的元信息到 `数据/爬取历史.json`：目标 URL、爬取时间、
    HTTP 状态码、响应耗时、字节大小、内容 SHA-256 哈希、抓取结果
    （新增 / 更新 / 未变化 / 失败）、错误原因

  - **增量爬取**：`run(incremental=True)` 时自动跳过 `incremental_max_age_hours`
    （默认 24 小时）内已成功抓取且内容未变化的章节，减少重复请求与资源消耗；
    跳过章节不重新请求、不写入输出（建议配合 `resume=True` 复用旧文件）

  - **内容变化检测**：每次抓取计算 SHA-256 哈希并与上次比较，自动标记
    新增 / 更新 / 未变化 / 失败 四种结果类型

  - **查询与统计 API**：`查询()` 支持按域名 / 时间范围 / 抓取结果过滤；
    `统计()` 输出各结果类型计数；`查URL()` / `查站点()` / `列出全部站点()`
    便于排查失败站点

  - **并发安全**：单例 + `threading.Lock` + 原子写入（临时文件 + `os.replace`），
    写入失败静默降级，绝不中断主爬取任务；每站点 URL 记录 LRU 截断（默认 5000 条）

### 功能优化

- `爬虫.py` 在 `_get_with_js_challenge`、`get_chapter_content` 分页请求、
  AJAX 两步加载（`sign_url` / `content_url`）三处入口统一接入历史记录，
  覆盖目录页 / 章节页 / 动态加载全路径

- 质检汇总报告新增「爬取历史统计」与「增量跳过章节」两行，便于复盘请求分布

- `run()` 新增 `incremental` / `incremental_max_age_hours` 两个可选参数，
  默认 `False` 保持原有全量抓取行为，对外接口完全向后兼容

### 问题修复

- 修复 404 末页与非 200 响应未写入历史导致下次仍重复请求的问题
  （分页探测的 404 现在也会记录为失败，便于排查）

### 其他

- 新增模块：[源码/爬取历史.py](源码/爬取历史.py)（纯标准库实现，无新依赖）

***

## \[2.0.1] - 2026-09-02

### 新增功能

- **打包脚本自动版本号管理**（[脚本/build\_exe.py](脚本/build_exe.py)）

  - 每次打包自动递增修订号（patch：2.0.0 → 2.0.1），并同步更新
    CHANGELOG 顶部最新条目的版本号与日期

  - 支持参数控制：`--bump=minor|major`（递增次/主版本）、`--version=x.y.z`（手动指定）、
    `--no-bump`（保持版本，重跑修复时用）

  - 版本状态持久化在 `脚本/版本.json`，EXE 属性自动显示当前版本号

### 功能优化

- 打包流程由 `flet pack` 改为直接调用 PyInstaller 并嵌入版本资源文件，
  修复 flet pack（0.86）忽略版本参数导致 EXE 无版本号的问题

- 主流程纳入 `main()` 保护，避免被其他模块导入时误触发打包

***

## \[2.0.0] - 2026-09-02

### 新增功能

- **反爬机制自动检测器**（[源码/反爬检测器.py](源码/反爬检测器.py)）

  - 基于「状态码 + 响应头 + 响应体特征」通用识别 6 种反爬机制：
    `rate_limit`（频率限制）、`ua_block`（User-Agent 校验）、`waf_captcha`（WAF 图片验证码）、
    `waf_js_challenge`（WAF JS 令牌挑战）、`js_cookie`（JS Cookie 校验）、`dynamic_token`（动态令牌/CSRF）

  - 输出结构化识别结果（机制 / 置信度 / 命中证据 / 建议策略），支持指数退避秒数计算

- **内容语义质检器**（[源码/内容质检器.py](源码/内容质检器.py)）

  - 五维量化评分：有效长度 / 中文占比 / 乱码率 / 重复率 / 标点密度

  - 硬伤判定：乱码率 > 30% 或中文占比 < 30% 直接判无效

  - 实时过滤无意义字符、乱码、空白噪声与重复噪声，仅保留有效正文

- **站点抓取历史记录**（[源码/站点历史.py](源码/站点历史.py)）

  - 跨会话持久化到 `数据/站点历史.json`（原子写入、线程安全）

  - 按站点记录：任务数、首次/最近抓取时间、反爬机制命中统计、书籍清单、质检通过率

- **多引擎请求**（[源码/请求引擎.py](源码/请求引擎.py)，集成成熟反爬库）

  - `curl_cffi`：模拟 Chrome TLS 指纹 + HTTP/2，绕过基于 TLS 指纹的校验

  - `cloudscraper`：自动执行 Cloudflare / DDoS-Guard 等 JS 质询，无需浏览器

  - 自动降级链：`requests → cloudscraper → curl_cffi`（连续失败自动降级）

### 功能优化

- `_get_with_js_challenge` 重构：每次请求先过反爬检测器，命中率限制自动指数退避
  （优先读取 `Retry-After` 头）、命中 UA 拦截自动轮换 User-Agent

- 章节抓取接入语义质检：质检未通过自动清除内容模式缓存并轮换 UA 重试（最多 2 次），
  并发与串行抓取路径统一接入

- 整书抓取完成后生成**质检汇总报告**（控制台输出 + 保存为 `书名.txt.质检报告.txt`），
  包含通过/未通过章节数、平均得分、反爬对抗统计、多引擎使用统计

- 抓取前打印**站点历史先验**，对存在频率/UA 类反爬历史的站点自动给出延迟建议

- 站点配置支持 `anti_spider: auto` 自动检测类型（未知站点无需手配反爬参数）

### 问题修复

- 修复同一 UA 连续 403/406 被拦截后无应对措施的问题（新增 UA 轮换机制）

- 修复频率限制（429）未识别、无退避导致请求持续被拒的问题（新增指数退避）

- 修复乱码页面因字符长度达标而误判为有效内容的问题（新增乱码率/中文占比硬伤判定）

- 修复不同任务间反爬/质检统计相互串扰的问题（任务级统计重置）

### 其他

- 新增依赖：`curl_cffi`、`cloudscraper`（见 [requirements.txt](requirements.txt)）

- 新增模块：反爬检测器 / 内容质检器 / 站点历史 / 请求引擎

- 打包版本号升级为 2.0.0，并在打包脚本中集中定义便于后续维护

***

## \[1.1.0] - 2026-08-12

### 新增功能

- WAF 双通道反爬：WAF JS 令牌挑战（`@wafjs`）自动渲染解决 + WAF 图片验证码自动识别

- 任务管理增强：支持重新下载、任务列表按需重建、删除确认

### 功能优化

- GUI 界面优化：字体规范化、莫兰迪配色丰富化、状态栏动态化、结果预览新增「打开输出文件夹」

### 问题修复

- 修复任务删除按钮（适配 Flet 0.86 `show_dialog` API 变更）

- 修复 4 个站点的抓取问题与同名小说序号并发竞态

- 打包补充 ddddocr 模型与 selenium 子模块，修复 EXE 内 WAF 验证码识别与浏览器兜底失效

***

## \[1.0.0] - 2026-08-11

### 新增功能

- 多站点小说爬虫：requests + BeautifulSoup4 + Selenium，支持断点续传、进度条、
  章节区间抓取、并发抓取、TXT 输出

- Flet 图形界面（GUI）：抓取 / 结果预览 / 站点配置 / 运行日志四页签，深浅色主题切换

- 站点适配器架构（[文档/SITE\_ADAPTER.md](文档/SITE_ADAPTER.md)）：支持 Base64 加密、
  AJAX 两步加载、HTML 选择器等多种内容模式，可扩展新站点

### 功能优化

- EXE 打包管道完善（flet pack + PyInstaller）：Flet client 打包进 EXE、
  统一打包后路径解析（避免落到 `_MEIPASS` 临时目录）

- 项目目录按功能模块分类重组：源码 / 脚本 / 文档 / 配置 / 日志

### 其他

- 首次发布，对应初始提交 `9e64954`（多站点小说爬虫 + Flet 图形界面，Windows EXE 支持）

