# 小说爬虫项目 — 全面检查与优化报告

> 生成时间：2026-08-11  
> 项目路径：K:\程序文件\小说爬虫

---

## 一、项目文件分类

### 核心源码（src/）

| 文件 | 大小 | 功能 |
|------|------|------|
| `爬虫.py` | 4440 行 | 主爬虫程序：NovelSpider 类、目录解析、正文抓取、分页合并、断点续传 |
| `sites_config.py` | 21.4 KB | 站点适配模式库：4 种模式常量、13 个站点配置 |
| `captcha_module.py` | 48 KB / 1152 行 | 验证码处理：可插拔策略（ddddocr/slider/third_party/point_click/manual） |
| `content_decoder.py` | 12 KB | 通用正文解码器：数据文件引用探测 + 码点流解析 |
| `decrypt_utils.py` | 12.4 KB | 正文解密工具库：6 种加密机制自动检测+解密 |
| `browser_driver.py` | 13 KB | Playwright 驱动的 Selenium 兼容封装，含 stealth 反检测 |
| `tanmixs_xs.py` | 6.4 KB | tanmixs .xs 正文数据文件解析器 |
| `_path_utils.py` | 3.6 KB | 统一路径解析模块（源码/PyInstaller onefile/onedir 三模式） |

### GUI 模块（src/gui_components/）

| 文件 | 功能 |
|------|------|
| `gui_app.py` | Flet GUI 主程序入口 |
| `task_manager.py` | 多线程任务管理（TaskLogRedirector 重定向 print 跟踪进度） |
| `crawl_tab.py` | 抓取选项卡 UI |
| `preview_tab.py` | 预览选项卡 UI |
| `config_tab.py` | 配置选项卡 UI |

### 辅助文件

| 文件 | 功能 |
|------|------|
| `启动爬虫.bat` / `启动GUI.bat` | Windows 启动脚本 |
| `打包EXE.bat` / `打包EXE.ps1` / `build_exe.py` | PyInstaller 打包脚本 |
| `小说爬虫.spec` | PyInstaller 规格文件 |
| `requirements.txt` | Python 依赖清单 |
| `ensure_flet_cache.py` | Flet 缓存初始化 |
| `README.md` / `LICENSE` | 文档与许可证 |
| `文档/` | 项目文档（3 个 .md） |
| `抓取结果/` | 输出目录（9 个 .txt） |
| `_flet_client/` | Flet Flutter 渲染引擎（41 个运行时文件） |

### 已清理的无用文件

| 清理项 | 说明 |
|--------|------|
| `__pycache__/` 目录 | Python 编译缓存（已删除） |
| `build/` 目录 | PyInstaller 构建中间产物（已删除） |
| `gui_run.log` / `gui_run2.log` | GUI 运行日志（已删除） |
| `build_log.txt` | 构建日志（已删除） |
| `optimize_crawler.py` / `fix_orphaned.py` | 临时优化脚本（已删除） |

---

## 二、代码优化清单

### 已完成的优化（爬虫.py：4977 行 → 4440 行，减少 537 行，-10.8%）

#### 第一轮优化（4977 → 4532，减少 445 行）

| # | 优化项 | 减少行数 | 说明 |
|---|--------|----------|------|
| 1 | 删除 3 个"向后兼容备用分支"死代码 | ~89 行 | qsbs_bb/11bzw.org/yqyp.net 域名分支已被通用层覆盖 |
| 2 | 清理优化脚本残留的孤立代码 | ~183 行 | 优化脚本删除分支外层结构后遗留的内部代码体 |
| 3 | 合并重复的编码检测逻辑 | ~80 行 | pjxdd.com 等网站的 if/else 重复编码检测合并为统一块 |
| 4 | 提取 `_extract_base64_blocks` 公共方法 | ~50 行 | 从 _extract_qsbs_bb_generic 和 _extract_str_decode_generic 中提取 |
| 5 | 添加模块级 `_CONTENT_FILTER_KEYWORDS` 常量 | ~40 行 | 从 clean_content 中提取内联过滤关键词到模块级 |
| 6 | 提取 `_selenium_get_soup` 公共方法 | ~60 行 | 统一 Selenium Chrome 选项设置、JS cookie 校验反爬处理 |
| 7 | 替换 inspect_page 中两处重复 Selenium 代码 | ~43 行 | 改为调用 `_selenium_get_soup` 方法 |

### 新增/修改的方法

- **`_extract_base64_blocks(url, headers, pattern, tag)`** — 公共 Base64 解码方法
- **`_selenium_get_soup(url, headers)`** — 统一 Selenium 抓取方法（创建无头 Chrome → 访问 → 处理反爬 → 返回 BeautifulSoup）
- **`_CONTENT_FILTER_KEYWORDS`** — 模块级常量，统一管理内容过滤关键词

---

## 三、端到端测试结果

### 测试环境
- Python 3.13.12（独立虚拟环境）
- 已安装核心依赖：requests, beautifulsoup4, lxml, fake-useragent, urllib3, certifi
- 未安装（可选）：selenium, playwright, flet, ddddocr, onnxruntime

### 测试结果汇总

| # | 测试项 | 结果 | 说明 |
|---|--------|------|------|
| 1 | 模块导入与类实例化 | ✅ 通过 | NovelSpider 实例化成功，session/验证码模块正常加载 |
| 2 | clean_content 内容清洗 | ✅ 通过 | 广告过滤、零宽字符清理、段落去重均正常 |
| 3 | clean_chapter_title 标题清理 | ✅ 通过 | 空格修剪、格式保留正常 |
| 4 | URL 安全校验（SSRF 防护） | ✅ 通过 | 公网地址放行；127.0.0.1/192.168/10.0/ftp:// 全部拦截 |
| 5 | _chapter_sort_key 章节排序 | ✅ 通过 | 排序结果：楔子→第1章→第2章→第3章→番外 |
| 6 | sites_config 站点适配 | ✅ 通过 | 13 个站点配置，11bzw.org/28zw.org 正确匹配 |
| 7 | decrypt_utils 解密工具 | ✅ 通过 | detect_std_base64/decrypt_content 函数正常 |
| 8 | content_decoder 数据文件解码 | ✅ 通过 | 无效 URL 优雅返回空结果，不崩溃 |
| 9 | _path_utils 路径解析 | ✅ 通过 | RESOURCE_DIR/BASE_DIR/OUTPUT_DIR 路径正确 |
| 10 | tanmixs_xs 解析器 | ✅ 通过 | parse_xs/fetch_chapter_content 函数可用，空输入不崩溃 |
| 11 | GUI 模块文件检查 | ✅ 通过 | 5 个 GUI 文件全部存在且语法正确 |
| 12 | 命令行入口 --help | ✅ 通过 | 正常显示用法说明 |
| 13 | captcha_module 语法 | ✅ 通过 | 语法检查通过 |
| 14 | browser_driver 语法 | ✅ 通过 | 语法检查通过 |
| 15 | 断点续传路径计算 | ✅ 通过 | _checkpoint_paths 正常返回路径 |
| 16 | deduplicate_paragraphs | ✅ 通过 | 方法调用无异常 |
| 17 | get_novel_title 书名提取 | ✅ 通过 | 成功从 example.com 提取标题 |

### 结论：**全部 17 项测试通过，无报错。**

---

## 三·A、运行测试（实际启动）

### Python 环境
- 项目本地 `.venv`（`K:\程序文件\小说爬虫\.venv\Scripts\python.exe`，flet 0.86.5）
- 启动脚本 `启动GUI.bat` / `启动爬虫.bat` 优先使用此 venv

### CLI 命令行模式
| 命令 | 结果 | 说明 |
|------|------|------|
| `python 爬虫.py --help` | ✅ 通过 | 帮助信息正常 |
| `python 爬虫.py <url> --list` | ✅ 通过 | 修复 11bzw.org URL 正则后，45 章节全部解析 |
| `python 爬虫.py <url> --test` | ✅ 通过 | 抓取第 1 章正文 2398 字符 |

### GUI 模式（flet_desktop）
| 测试项 | 结果 | 说明 |
|--------|------|------|
| 启动 `gui_app.py` | ✅ 通过 | flet_desktop 正常弹出窗口 |
| `crawl_tab.build()` 同步构建 | ✅ 通过 | URL 输入、模式 Dropdown、按钮组、进度条、日志区全部正常 |
| `preview_tab.build()` 同步构建 | ✅ 通过 | 文件列表、预览面板、按钮全部正常 |
| `config_tab.build()` 同步构建 | ✅ 通过 | 站点配置 UI 正常 |

### 下午修复的 3 个新问题（运行测试中发现）
| # | 问题 | 严重性 | 修复 |
|---|------|--------|------|
| 1 | 11bzw.org URL 正则只匹配 `/index/{aid}/`，实际网站用 `/book/{aid}/`，导致"--list"返回 0 章 | 中 | ✅ 修复正则，同时匹配两种格式 |
| 2 | flet 0.86.5 API 变更：`ft.Tabs(tabs=[ft.Tab(content=...)])` 拆分，必须用 `ft.Tabs(length=N) + ft.TabBar + ft.TabBarView` | 中 | ✅ 重写 `gui_app.py` 的 Tabs 调用 |
| 3 | flet 0.86 移除 `Button.text_size` 参数 | 高 | ✅ 替换全部 22 处为 `style=ft.ButtonStyle(text_style=ft.TextStyle(size=N))` |

---

## 四、发现的问题清单

### 已修复的问题

| # | 问题 | 严重性 | 状态 |
|---|------|--------|------|
| 1 | 3 个域名分支死代码（已被通用层覆盖但未删除） | 中 | ✅ 已删除 |
| 2 | 优化脚本残留 183 行孤立代码导致 IndentationError | 高 | ✅ 已清除 |
| 3 | `_selenium_get_soup` 方法被调用但定义缺失 | 高 | ✅ 已补全 |
| 4 | 编码检测逻辑在多个域名分支中重复 | 低 | ✅ 已合并 |
| 5 | Base64 解码逻辑在两个方法中重复 | 低 | ✅ 已提取公共方法 |
| 6 | 内容过滤关键词内联定义 | 低 | ✅ 已提取为模块级常量 |
| 7 | inspect_page 中 Selenium 代码重复 | 低 | ✅ 已提取公共方法 |

#### 第二轮优化（4532 → 4440，减少 92 行）

| # | 优化项 | 详情 |
|---|--------|------|
| 8 | `_looks_like_content()` 重复定义消除 | `decrypt_utils.py` 改为引用 `content_decoder._looks_like_content`，保留 ImportError 兜底 |
| 9 | 码点流解析逻辑去重 | `tanmixs_xs.parse_xs()` 改为调用 `content_decoder.parse_codepoint_stream()`，消除 ~35 行重复 |
| 10 | `_json_safe()` 重复消除 | `tanmixs_xs.py` 改为引用 `content_decoder._json_safe` |
| 11 | 命令行参数解析去重 | 提取 `_parse_batch_opts()` 公共函数，`--batch` 和多 URL 模式共用（消除 ~60 行重复） |
| 12 | `get_chapter_content` 内联 Selenium 复用 | 替换为调用 `_selenium_get_soup()`，消除 ~50 行重复 Chrome 配置代码 |
| 13 | 冗余调试 print 清理 | 删除 9 行纯调试输出（"方法N"标签、"等待N秒后重试"等） |
| 14 | bat 文件编码防护 | `启动爬虫.bat` 和 `启动GUI.bat` 改用 `chcp 65001`（UTF-8），防止中文 echo 乱码 |

### 待处理的问题（不影响正常运行）

| # | 问题 | 严重性 | 建议 |
|---|------|--------|------|
| 1 | ~~`content_decoder.py` 和 `decrypt_utils.py` 有重复的 `_looks_like_content()`~~ | — | ✅ 第二轮已修复 |
| 2 | ~~`content_decoder.py` 和 `tanmixs_xs.py` 有重复的码点流解析逻辑~~ | — | ✅ 第二轮已修复 |
| 3 | ~~命令行参数解析在三处有重复逻辑~~ | — | ✅ 第二轮已修复 |
| 4 | ~~`get_chapter_content` 中的内联 Selenium 块未使用 `_selenium_get_soup`~~ | — | ✅ 第二轮已修复 |
| 5 | ~~大量冗余调试 print 输出~~ | — | ✅ 第二轮已清理 |
| 6 | `get_chapter_list` 方法仍有 7 个域名特判分支 | 中 | 已评估：各分支解析逻辑完全不同（onclick/全文目录/Selenium渲染/JS分页），迁移到 sites_config 需设计策略模式接口，风险高收益低，保持原样 |
| 7 | `.mimosa/` 目录（69 个文件）未清理 | 低 | 被安全机制拦截，可手动清理 |
| 8 | 文件名混用中英文（爬虫.py vs sites_config.py） | 低 | 建议统一命名规范 |
| 9 | `deduplicate_paragraphs` 对短文本去重效果不明显 | 低 | 可调整去重算法阈值 |

---

## 五、优化前后对比

| 指标 | 优化前 | 优化后 | 变化 |
|------|--------|--------|------|
| 爬虫.py 行数 | 4977 | 4440 | -537 行（-10.8%） |
| 死代码分支 | 3 个 | 0 | 全部清除 |
| 重复方法 | 8 处 | 0 | 全部提取/引用 |
| 模块级常量 | 0 | 1 | _CONTENT_FILTER_KEYWORDS |
| 公共方法 | 0 | 4 | _extract_base64_blocks / _selenium_get_soup / _parse_batch_opts / parse_codepoint_stream(统一引用) |
| 跨模块重复函数 | 3 处 | 0 | _looks_like_content / _json_safe / 码点流解析 全部统一引用 |
| bat 编码防护 | chcp 936 | chcp 65001 | UTF-8 防乱码 |
| 冗余调试 print | 9 处 | 0 | 已清理 |
| 语法错误 | 0 | 0 | 无 |
| 测试通过率 | — | 100% | CLI + GUI + 模块导入 全通过 |

---

## 六、建议的后续优化方向

1. ~~**统一日志系统**：将 print 替换为 logging 模块~~ — 部分完成（已清理冗余 print，剩余为用户可见进度信息，保留）
2. ~~**域名配置化**：将 `get_chapter_list` 中的域名特判逐步迁移到 `sites_config.py`~~ — 已评估，风险高收益低，保持原样
3. ~~**公共工具模块**：提取 `_looks_like_content`、码点流解析等重复逻辑到独立工具模块~~ — ✅ 已完成
4. ~~**Selenium 代码统一**：将 `get_chapter_content` 中的内联 Selenium 块也替换为 `_selenium_get_soup` 调用~~ — ✅ 已完成
5. **类型注解**：为核心方法添加 type hints，提升可读性和 IDE 支持
6. **单元测试**：为核心方法（clean_content、_chapter_sort_key、validate_public_url 等）编写正式的 pytest 测试用例
7. **bat 编码防护**：~~已完成~~ ✅ `chcp 65001` 已应用到两个 bat 文件
