# 本地项目清理日志 — 2026-09-13

> 执行者：Claude（Trae 会话）· 依据：用户"系统性整理与清理"指令
> 方式：先全面扫描盘点 → 不确定项（4 项）逐条询问用户裁决 → `shutil.rmtree` 批量执行
> （规避删除钩子按轮计额，见 踩坑总表 K2）→ 门禁验证功能无损
> 清理前状态：`main @ 2bc1b19` = origin/main（与 GitHub 一致，已另行核查）

---

## 一、已删除（21 项，全部成功）

### 其他会话遗留（用户指令①）

| 路径 | 内容 | 文件数 |
|---|---|---|
| `.workbuddy/` | WorkBuddy 会话本地记忆（MEMORY.md 长期约定 / plans / cache / artifacts）——内容已被 `文档/踩坑总表.md` 与各审查报告汲取 | 27 |
| `抓取结果（测试用）/` | 某会话创建的测试输出目录，实际仅含 `.mimosa` 审查缓存，无任何抓取产物 | 2 |
| `源码/.mimosa/` | AI 审查工具（mimosa）缓存：hook-state / finding-ledger / reports | 51 |
| `源码/gui_components/.mimosa/` | 同上 | 5 |
| `界面设计预览/.mimosa/` | 同上 | 1 |

### 用户裁决删除（不确定项询问后确认）

| 路径 | 内容 | 裁决 |
|---|---|---|
| `.idea/` | JetBrains/PyCharm 项目配置（workspace/iml 等，gitignored） | 删除 |
| `实际程序图/` | 用户反馈用程序截图素材（0.7MB，gitignored） | 删除 |
| `预览图/` | 用户反馈用预览截图素材（0.4MB，gitignored） | 删除 |

### 无用文件（临时/日志/再生成物）

| 路径 | 内容 | 说明 |
|---|---|---|
| `日志/` | 运行日志 16.8MB（11 文件） | 程序按日重建，保留 30 天机制 |
| `build_log.txt` | PyInstaller 打包日志 | 打包时再生成 |
| `小说爬虫.spec` | PyInstaller spec | 再生成物，build_exe.py 直接传参不用 spec |
| `__pycache__/` ×8 | Python 字节码缓存（源码/测试/站点适配/脚本/远控/dist 内） | 运行时自动再生 |
| `脚本/_cleanup_scan.py`、`脚本/_cleanup_execute.py` | 本次清理的临时脚本 | 用后自删 |

**合计释放**：约 17.5 MB（不含已裁决保留项）。

---

## 二、不确定项裁决记录（逐条询问用户）

| 路径 | 作用说明 | 用户裁决 |
|---|---|---|
| `rust_core_poc/.toolchain/`（**1.25GB**） | Rust 工具链（stable-x86_64-pc-windows-msvc），**本机唯一**（`~/.rustup/toolchains` 为空）——本地重编译 `rust_core.pyd` 依赖它 | **保留** |
| `.workbuddy/`（0.3MB） | WorkBuddy 会话记忆 | 删除 |
| `.idea/` | PyCharm IDE 配置 | 删除 |
| `实际程序图/`+`预览图/`（1.1MB） | 用户截图素材 | 删除 |

## 三、保留说明（非垃圾，勿在后续清理中误删）

| 路径 | 保留理由 |
|---|---|
| `rust_core_poc/.toolchain/`（1.25GB） | 用户裁决保留；本机唯一 Rust 工具链 |
| `_flet_client/`（95.9MB） | 打包必需的 Flet 客户端缓存（build_exe.py 引用；删后需重新下载） |
| `dist/`（284MB） | 本机在用的 v2.4.28 EXE + 旁挂站点适配/用户配置 |
| `数据/`、根级 `captcha_config.json`、`站点配置.json` | 运行时用户状态与本机配置（ddddocr 显式开启等，勿当漂移修复） |
| `源码/rust_core.pyd` | 在用的 Rust 加速产物（打包收集项） |
| `HANDOFF.md` | 跨会话交接文件（gitignored，不入库） |

## 四、清理后验证

- `unittest discover`：**247 tests OK**；`check_undefined_refs.py`：干净（清理未触碰任何源码）
- git 状态：仅 AGENTS.md 规约新增与本文档变更，与清理动作零关联（清理项全部在 .gitignore 覆盖范围内或未跟踪）

## 五、配套沉淀

- 上传规约定稿：见 `AGENTS.md` §文件上传规约（入库白名单 / 禁止入库四类 / 提交纪律）
- HANDOFF.md 同步：移除对已删 `.workbuddy/` 的引用（上手顺序与核心文件路径表）
