# 项目目录整理方案 (2026-09-03, 待确认执行)

> 原则: 这是**活跃工程**, 目录布局承载运行时约定 (启动器路径/数据查找/打包引用)。
> 整理 = 清除真正的冗余产物 + 少量归位; **不做大挪移**, 避免破坏运行链路。
> ⚠ 项目存在并行会话, 执行前需确认无其他会话正在写文件。

---

## 一、现状用途总表 (扫描结果)

### 根目录文件 (14 个)

| 文件 | 用途 | 状态 |
|---|---|---|
| .gitignore / README.md / LICENSE / CHANGELOG.md / requirements.txt | 工程标准件 | ✅ 保留 |
| 启动GUI.bat / 启动爬虫.bat | 用户入口 (引用 .venv + 源码/) | ✅ 保留 |
| 小说爬虫.spec | PyInstaller 打包配置 (git 跟踪) | ✅ 保留 |
| captcha_config.json | **运行时副本** (与 配置/ 模板 md5 相同) | ✅ 保留(程序生成) |
| 站点配置.json | 站点管理 GUI 运行数据 (gitignore) | ✅ 保留 |
| _version_info.txt | build_exe 版本状态 (**删了版本号回退**) | ✅ 保留 |
| build_log.txt | 上次构建日志 18KB | 🗑 可删 |
| 提示词.txt | 私人文件 (gitignore) | ✅ 保留 |
| 常用网址.txt | 9 个测试站点 URL (git 已跟踪) | 📦 建议归位 数据/ |

### 目录 (16 个)

| 目录 | 大小 | 用途 | 状态 |
|---|---|---|---|
| .venv | 624M | Python 虚拟环境 (运行必需) | ✅ |
| dist | 383M | **发布产物**: 小说爬虫.exe v2.2.0 + 运行时目录(数据/日志/抓取结果/配置) | ✅ 保留* |
| **build** | **346M** | PyInstaller 中间产物 (旧) | 🗑 **冗余, 可删** |
| **build_v2.2.0** | **346M** | PyInstaller 中间产物 (v2.2.0 构建) | 🗑 **冗余, 可删** |
| _flet_client | 97M | flet 桌面客户端 (打包资源, spec 引用) | ✅ |
| 源码 | 2.5M | 36 个 py (核心引擎+GUI), bat/spec/脚本 全部引用 | ✅ |
| 脚本 | 225K | 12 个工具 (build_exe/monitor_summary/probe_adapter/单测×2/迁移脚本) | ✅ |
| 测试 | 90K | 3 个 py (29 例离线回归) | ✅ |
| 测试样本 | 697K | 20 个站点快照 (回放回归基准) | ✅ |
| 文档 | 84K | 8 份 (审查报告/SITE_ADAPTER/设计文档×2/评估/验证码说明×2/日志说明) | ✅ |
| 日志 | 792K | CLI 运行日志 (30 天自动轮转) | ✅ |
| 数据 | 122K | 运行数据 (爬取/站点历史、域状态、速度画像) | ✅ |
| 抓取结果 | 21K | 已抓小说成品 | ✅ |
| 配置 | 4K | captcha_config 模板 (启动时复制到运行位置) | ✅ |
| .idea / .workbuddy | 17K/48K | IDE / WorkBuddy 会话数据 | ✅ (gitignore) |

### dist/ 内的运行时目录 (exe 运行时生成, 29 项)

`dist/数据`(4)、`dist/日志`(**12 个轮转 log**)、`dist/抓取结果`(少年阿宾.txt 等)、
`dist/captcha_config.json`、`dist/站点配置.json` — **EXE 的运行环境**, 动了会丢
exe 侧的续传进度与历史。建议保留; 若想清爽, 应改用"每次从项目根运行源码版"。

## 二、目标结构

**维持现有布局** (已是规范工程结构: 源码/脚本/测试/测试样本/文档/配置/数据/日志/抓取结果 九区 + 根部标准件)。
不新建/合并目录 — 一切散文件要么是程序约定路径, 要么是 gitignore 管理的运行产物。

## 三、冗余 / 过期 / 重复标记

| 标记 | 对象 | 说明 |
|---|---|---|
| 🗑 冗余 | `build/` + `build_v2.2.0/` (692MB) | PyInstaller 中间产物, 下次打包自动重建; 删除仅损失增量编译速度 |
| 🗑 过期 | `build_log.txt` (18KB) | 旧构建日志, gitignore 已忽略 |
| 🟡 可选 | 47 个 `__pycache__/*.pyc` | Python 自动缓存 (gitignore), 删了会再生, 清不清均可 |
| 🔁 重复(约定) | `配置/captcha_config.json` ≡ 根 `captcha_config.json` (md5 相同) | **程序设计使然**: 模板→运行时复制, 删除会再生成, 勿动 |
| 🔁 重复(约定) | `dist/数据`、`dist/日志`、`dist/站点配置.json` 等 ≡ 根目录同名 | EXE 运行环境副本, exe 续传/历史依赖它们 |
| 🟡 归位 | `常用网址.txt` (根) | 用户测试 URL 清单, 建议 `git mv` 到 `数据/` (运行数据类) |

## 四、操作清单 (待确认)

### 批次 A — 删除冗余 (释放 ~692MB, 零风险)
1. `rm -rf build/` (346MB)
2. `rm -rf build_v2.2.0/` (346MB)
3. `rm build_log.txt` (18KB, 可选)
4. (可选) 清 47 个 pyc 缓存

### 批次 B — 归位 (可选, 1 项)
5. `git mv 常用网址.txt 数据/测试网址.txt` — 归入运行数据区 (引用检查: 无代码引用此文件名, 零影响)

### 明确不做 (及原因)
- 不移动 源码/脚本/测试/配置/数据/日志/抓取结果 任何目录 — bat/spec/build_exe/_path_utils 均按现路径引用
- 不删 dist/ 内运行时目录 — exe 续传与历史依赖
- 不删 _version_info.txt — 版本号会回退
- 不删两份 captcha_config — 程序约定复制

## 五、执行前置条件
- [ ] 确认并行会话当前无打包/抓取任务在跑
- [ ] (可选) 打包前如需保留 build 缓存加速, 可只删 build_v2.2.0 留 build
