# HANDOFF.md — 项目交接文件

> **本文件是跨 Agent 交接的唯一事实来源。**
> zCode / WorkBuddy / 其他 AI Agent 交替开发时，开工先读本文件，收工必更新本文件。

---

## 使用规则（所有 Agent 必须遵守）

1. **开工第一件事**：完整阅读本文件，了解项目背景、已完成进度、当前停留点和踩坑记录。
2. **每完成一个可交付的工作单元后**（一个功能 / 一个 bug 修复 / 一次调试闭环），必须更新下方「动态信息」区，并更新顶部元信息的「最后更新时间」和「最后更新者」。
3. **禁止**等到项目结束或额度快耗尽时才一次性写交接文件。
4. **禁止**跳过本文件更新直接开始下一个任务。
5. 本文件为个人交接文件，**不提交到项目公共 Git 仓库**（请在 `.gitignore` 中忽略）。

---

## 元信息

| 字段 | 内容 |
|---|---|
| 项目名称 | （填写项目名） |
| 项目路径 | （填写本地绝对路径） |
| 最后更新时间 | YYYY-MM-DD HH:MM |
| 最后更新者 | zCode / WorkBuddy / 其他 |
| 文档版本 | v1.0 |

---

## 一、不变信息（写一次即可，项目结构变化时才更新）

### 1. 项目背景与目标
（一句话描述这个项目是做什么的、要解决什么问题）

### 2. 技术栈
- 语言：
- 框架：
- 数据库：
- 其他关键依赖：

### 3. 运行环境
- 操作系统：
- 运行命令：（如 `npm run dev` / `python main.py`）
- 环境变量：（列出需要配置的 key，值不要写在这里）
- 端口：

### 4. 核心文件路径
| 文件/目录 | 作用 |
|---|---|
| `src/` | （说明） |
| `config/` | （说明） |
| （补充） |  |

### 5. Git 信息
- 当前分支：
- 远程仓库：
- 依赖版本锁定文件：（`package-lock.json` / `requirements.txt` / `poetry.lock` 等）

---

## 二、动态信息（每次工作完成后必须更新）

### 已完成工作
> 格式：`- [x] YYYY-MM-DD 完成内容（执行者）`

- [x] （示例）2026-09-11 初始化项目结构，配置 ESLint（zCode）
- [x] （示例）2026-09-11 实现用户登录接口（WorkBuddy）

### 当前进度
- **正在做**：（描述当前进行到哪一步）
- **停在**：（具体到文件/函数/行，例如「controller 层写完，service 层未测试」）
- **未完成模块**：（列出还没做的部分）

### 下一步待办（按优先级排序）
1. （最高优先级）
2.
3.

### 踩坑记录
> 记录所有失败方案、异常报错、规避方法，避免下一个 Agent 重蹈覆辙。

- **YYYY-MM-DD**（执行者）：
  - 问题：
  - 原因：
  - 解决方案：
  - （示例）MySQL 8.0 默认认证插件是 caching_sha2_password，旧客户端连不上，需改 mysql_native_password

---

## 三、交接检查清单（切换 Agent 前确认）

- [ ] 本文件「动态信息」区已更新到最新状态
- [ ] 顶部「最后更新时间」和「最后更新者」已更新
- [ ] 源代码已 Git 提交（如适用）
- [ ] 临时文件 / Agent 缓存已清理（如适用）
- [ ] 下一个 Agent 已知晓从哪里继续

---

*模板版本：v1.0 | 适用：zCode ↔ WorkBuddy 跨 Agent 交替开发*
